{% macro speaker(params) %}
  {% set imgSrc = "assets/img/speakers/" ~ params.image %}
  {% set imgSrcPixels = "assets/img/speakers/" ~ params.imagePixels %}
  <div class="c-speaker">
    <div class="c-speaker__media">
      <a class="c-speaker__link" href="{{ params.url }}">
        <img 
          src="{{ imgSrc }}"
          class="c-speaker__img  o-fluidimg"
          alt="{{ params.name }} {{ params.surname }}"
          width="800"
          height="800"
          loading="lazy"
          decoding="async">
      </a>
    </div>
    <h3 class="c-speaker__name">
      <a href="{{ params.url }}">
        {{ params.name }} {{ params.surname }}
      </a>
    </h3>
    <p class="c-speaker__function">
      {{ params.function }}
    </p>
  </div>
{% endmacro %}